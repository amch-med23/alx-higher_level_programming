-- top_score
-- lists allrecords of the second_table of the given database(passed as an argument to mysql), dislay both scors in the same line and order the records desplaying by the score value.
SELECT score, name FROM second_table ORDER BY score DESC;
