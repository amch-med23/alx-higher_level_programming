-- best_score
-- show the raws of second_table where score>=10 in the database(will be passed as an argument to my sql). following the same orders as the last task(10)
SELECT score, name FROM second_table WHERE score >= 10 ORDER BY score DESC;
